<Window x:Class="RimWorldFramework.GUI.GameWorldWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RimWorld 游戏世界" 
        Height="900" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Grid Background="#FF1A202C">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#FF2D3748" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🌍 RimWorld 游戏世界" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
                <Button Name="BackButton" 
                        Content="返回主菜单" 
                        Margin="20,0,0,0"
                        Padding="10,5"
                        Click="BackButton_Click"/>
                <Separator Margin="20,0" Background="Transparent"/>
                <TextBlock Name="InfoText" 
                           Text="地图: 256×256格 | 人物: 1个 | 状态: 已加载" 
                           Foreground="#FFCBD5E0" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- 游戏世界显示区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            
            <!-- 地图显示区域 -->
            <Border Grid.Column="0" Background="#FF2D3748" Margin="5">
                <ScrollViewer Name="MapScrollViewer" 
                              ZoomMode="Enabled" 
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto">
                    <Canvas Name="GameCanvas" 
                            Width="8192" 
                            Height="8192"
                            Background="Black">
                        <!-- 地图和人物将在这里动态生成 -->
                    </Canvas>
                </ScrollViewer>
            </Border>
            
            <!-- 控制面板 -->
            <Border Grid.Column="1" Background="#FF4A5568" Margin="5">
                <StackPanel Margin="10">
                    <TextBlock Text="游戏控制" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,10"/>
                    
                    <Button Name="StartButton" 
                            Content="▶ 开始游戏" 
                            Margin="0,5"
                            Padding="10,8"
                            Click="StartButton_Click"/>
                    
                    <Button Name="PauseButton" 
                            Content="⏸ 暂停游戏" 
                            Margin="0,5"
                            Padding="10,8"
                            IsEnabled="False"
                            Click="PauseButton_Click"/>
                    
                    <Button Name="ResetButton" 
                            Content="🔄 重置地图" 
                            Margin="0,5"
                            Padding="10,8"
                            Click="ResetButton_Click"/>
                    
                    <Separator Margin="0,10"/>
                    
                    <TextBlock Text="地图信息" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    
                    <TextBlock Name="MapInfoText" 
                               Foreground="#FFCBD5E0" 
                               TextWrapping="Wrap"
                               Text="大小: 256×256格&#x0a;像素: 32×32每格&#x0a;总大小: 8192×8192像素"/>
                    
                    <Separator Margin="0,10"/>
                    
                    <TextBlock Text="地形图例" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <Rectangle Width="20" Height="15" Fill="Black" Margin="0,0,10,0"/>
                            <TextBlock Text="深水/岩石" Foreground="White" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <Rectangle Width="20" Height="15" Fill="#FFFFEAA7" Margin="0,0,10,0"/>
                            <TextBlock Text="沙地/沙漠" Foreground="White" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <Rectangle Width="20" Height="15" Fill="#FF90EE90" Margin="0,0,10,0"/>
                            <TextBlock Text="草地/森林" Foreground="White" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <Rectangle Width="20" Height="15" Fill="White" Margin="0,0,10,0"/>
                            <TextBlock Text="雪地/高山" Foreground="White" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Separator Margin="0,10"/>
                    
                    <TextBlock Text="人物信息" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    
                    <TextBlock Name="CharacterInfoText" 
                               Foreground="#FFCBD5E0" 
                               TextWrapping="Wrap"
                               Text="数量: 1个&#x0a;位置: (128, 128)&#x0a;状态: 随机移动"/>
                    
                    <Separator Margin="0,10"/>
                    
                    <TextBlock Text="视图控制" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    
                    <Button Name="CenterViewButton" 
                            Content="📍 居中视图" 
                            Margin="0,5"
                            Padding="10,5"
                            Click="CenterViewButton_Click"/>
                    
                    <Button Name="FollowCharacterButton" 
                            Content="👤 跟随人物" 
                            Margin="0,5"
                            Padding="10,5"
                            Click="FollowCharacterButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#FF2D3748" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Name="StatusText" 
                           Text="状态: 游戏已加载" 
                           Foreground="White" 
                           Margin="0,0,20,0"/>
                <TextBlock Name="FpsText" 
                           Text="FPS: 60" 
                           Foreground="#FFCBD5E0"
                           Margin="0,0,20,0"/>
                <TextBlock Name="TimeText" 
                           Text="游戏时间: 00:00:00" 
                           Foreground="#FFCBD5E0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>