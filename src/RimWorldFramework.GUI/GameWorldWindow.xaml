<Window x:Class="RimWorldFramework.GUI.GameWorldWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RimWorld 游戏世界" 
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="False"
        AllowsTransparency="False"
        Background="#90EE90">
    
    <Grid>
        <!-- 全屏地图显示区域 -->
        <ScrollViewer Name="MapScrollViewer" 
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollBarVisibility="Hidden"
                      CanContentScroll="True"
                      Background="#90EE90">
            <Canvas Name="GameCanvas" 
                    Width="16384" 
                    Height="16384"
                    Background="#90EE90">
                <Canvas.RenderTransform>
                    <ScaleTransform x:Name="MapScaleTransform" ScaleX="1" ScaleY="1"/>
                </Canvas.RenderTransform>
                <!-- 地图和人物将在这里动态生成 -->
            </Canvas>
        </ScrollViewer>
        
        <!-- ESC菜单面板 (默认隐藏) -->
        <Border Name="EscMenuPanel" 
                Background="#CC000000" 
                Visibility="Collapsed"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border Background="#FF2D3748" 
                    CornerRadius="10"
                    Padding="30"
                    MinWidth="400">
                <StackPanel>
                    <TextBlock Text="🎮 游戏菜单" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>
                    
                    <!-- 游戏控制 -->
                    <TextBlock Text="游戏控制" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,10"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <Button Name="StartButton" 
                                Content="▶ 开始游戏" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                Click="StartButton_Click"/>
                        
                        <Button Name="PauseButton" 
                                Content="⏸ 暂停游戏" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                IsEnabled="False"
                                Click="PauseButton_Click"/>
                        
                        <Button Name="ResetButton" 
                                Content="🔄 重置地图" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                Click="ResetButton_Click"/>
                    </StackPanel>
                    
                    <!-- 视图控制 -->
                    <TextBlock Text="视图控制" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,10,0,10"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <Button Name="CenterViewButton" 
                                Content="📍 居中视图" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                Click="CenterViewButton_Click"/>
                        
                        <Button Name="FollowCharacterButton" 
                                Content="👤 跟随人物" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                Click="FollowCharacterButton_Click"/>
                    </StackPanel>
                    
                    <!-- 相机控制说明 -->
                    <TextBlock Text="相机控制" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,10,0,5"/>
                    
                    <TextBlock Foreground="#FFCBD5E0" 
                               FontSize="12"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Margin="0,0,0,15">
                        <Run Text="WASD / 方向键 - 移动视角"/>
                        <LineBreak/>
                        <Run Text="鼠标中键拖动 - 平移视角"/>
                        <LineBreak/>
                        <Run Text="鼠标滚轮 - 以鼠标为中心缩放"/>
                        <LineBreak/>
                        <Run Text="空格键 - 居中到人物"/>
                        <LineBreak/>
                        <Run Text="R键 - 重置缩放"/>
                        <LineBreak/>
                        <Run Text="F键 - 切换跟随模式"/>
                    </TextBlock>
                    
                    <!-- 游戏信息 -->
                    <Border Background="#FF4A5568" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Name="StatusText" 
                                       Text="状态: 游戏已加载" 
                                       Foreground="White" 
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,5"/>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Name="FpsText" 
                                           Text="FPS: 60" 
                                           Foreground="#FFCBD5E0"
                                           Margin="0,0,15,0"/>
                                <TextBlock Name="TimeText" 
                                           Text="游戏时间: 00:00:00" 
                                           Foreground="#FFCBD5E0"/>
                            </StackPanel>
                            
                            <TextBlock Name="CharacterInfoText" 
                                       Text="人物: 1个 | 位置: (128, 128) | 状态: 随机移动"
                                       Foreground="#FFCBD5E0" 
                                       FontSize="11"
                                       HorizontalAlignment="Center"
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 退出按钮 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Name="BackButton" 
                                Content="返回主菜单" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                Click="BackButton_Click"/>
                        
                        <Button Name="ExitFullscreenButton" 
                                Content="❌ 退出游戏" 
                                Margin="5"
                                Padding="15,8"
                                MinWidth="120"
                                Background="#FFE53E3E"
                                Foreground="White"
                                Click="ExitFullscreenButton_Click"/>
                    </StackPanel>
                    
                    <TextBlock Text="按 ESC 键关闭菜单" 
                               Foreground="#FFCBD5E0" 
                               FontSize="11"
                               HorizontalAlignment="Center"
                               Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>