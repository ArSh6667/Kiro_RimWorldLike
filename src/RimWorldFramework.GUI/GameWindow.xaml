<Window x:Class="RimWorldFramework.GUI.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RimWorld 游戏 - 运行中" 
        Height="700" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF1A202C" Offset="0"/>
                <GradientStop Color="#FF2D3748" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 游戏标题栏 -->
        <Border Grid.Row="0" Background="#FF2D3748" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🎮 RimWorld 游戏框架演示" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
                <Button Name="BackToMenuButton" 
                        Content="返回主菜单" 
                        Margin="20,0,0,0"
                        Padding="10,5"
                        Click="BackToMenuButton_Click"/>
            </StackPanel>
        </Border>
        
        <!-- 游戏内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 游戏演示区域 -->
            <Border Grid.Column="0" 
                    Background="#FF4A5568" 
                    CornerRadius="5" 
                    Margin="0,0,5,0">
                <ScrollViewer Name="GameOutputScrollViewer" 
                              VerticalScrollBarVisibility="Auto">
                    <TextBlock Name="GameOutputText" 
                               Foreground="White" 
                               FontFamily="Consolas" 
                               FontSize="12" 
                               Margin="10"
                               TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
            
            <!-- 控制面板 -->
            <Border Grid.Column="1" 
                    Background="#FF4A5568" 
                    CornerRadius="5" 
                    Margin="5,0,0,0">
                <StackPanel Margin="10">
                    <TextBlock Text="游戏控制" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,10"/>
                    
                    <Button Name="StartDemoButton" 
                            Content="启动演示" 
                            Margin="0,5"
                            Click="StartDemoButton_Click"/>
                    
                    <Button Name="StopDemoButton" 
                            Content="停止演示" 
                            Margin="0,5"
                            Click="StopDemoButton_Click"
                            IsEnabled="False"/>
                    
                    <Button Name="ClearOutputButton" 
                            Content="清空输出" 
                            Margin="0,5"
                            Click="ClearOutputButton_Click"/>
                    
                    <Separator Margin="0,10"/>
                    
                    <TextBlock Text="游戏信息" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    
                    <TextBlock Name="GameInfoText" 
                               Foreground="#FFCBD5E0" 
                               TextWrapping="Wrap"
                               Text="ECS架构游戏框架&#x0a;支持角色系统、任务系统、路径寻找等功能"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#FF2D3748" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Name="GameStatusText" 
                           Text="游戏状态: 已加载" 
                           Foreground="White" 
                           Margin="0,0,20,0"/>
                <TextBlock Name="GameTimeText" 
                           Text="运行时间: 00:00:00" 
                           Foreground="#FFCBD5E0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>